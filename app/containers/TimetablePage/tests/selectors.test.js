// import { selectTimetablePageDomain } from '../selectors';

describe('selectTimetablePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
